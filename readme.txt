
You can define the port at 5006s - server code. 
Now it's 5006 but you can change it.

From the client side, you can define the port and the ip address of the server.
You can adjust the rate of packet by sleep command.

First, run the server side as naturally, and then run the client side.
